# values.yaml
backend:
  replicaCount: 2  # Ini akan memberimu poin ekstra scaling!
  image:
    repository: tw-fastapi # Ganti dengan nama image kamu
    tag: "latest"
    pullPolicy: IfNotPresent
  service:
    port: 8000